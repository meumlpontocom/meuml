import { createContext } from "react";

const shopeeReplicateToMLContext    = createContext({});
export const { Provider, Consumer } = shopeeReplicateToMLContext;

export default shopeeReplicateToMLContext;
