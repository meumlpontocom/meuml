import { createContext } from "react";

const passwordRecoveryContext       = createContext({});
export const { Provider, Consumer } = passwordRecoveryContext;

export default passwordRecoveryContext;
